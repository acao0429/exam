/* ============================================================
   雲端設定（js/config.js）
   這裡「不放任何密碼」。
   老師的登入帳號由系統管理員在 D1 資料庫中建立。
   題庫同步透過老師登入後的 teacher session token 驗證。

   apiBase：雲端 API 位址。留空代表關閉雲端功能。
   ============================================================ */

window.APP_CONFIG = {
  apiBase: "https://exam-api.ples.workers.dev"
};
